global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'example-api'
    static_configs:
      - targets: ['api:9090']
        labels:
          service: 'example-api'
          environment: 'docker'
