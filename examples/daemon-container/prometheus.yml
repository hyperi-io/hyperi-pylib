global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'example-daemon'
    static_configs:
      - targets: ['daemon:9090']
        labels:
          service: 'example-daemon'
          environment: 'docker'
