name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

jobs:
  build:
    name: Build
    uses: ./ci/workflows/build.yml@main
    secrets: inherit

  publish:
    name: Publish to JFrog
    needs: build
    uses: ./ci/workflows/publish.yml@main
    with:
      artifact-name: dist-python
    secrets: inherit
