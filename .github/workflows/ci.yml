name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  quality:
    name: Quality Checks
    uses: ./ci/workflows/quality.yml
    secrets: inherit

  build:
    name: Build
    needs: quality
    uses: ./ci/workflows/build.yml
    secrets: inherit
