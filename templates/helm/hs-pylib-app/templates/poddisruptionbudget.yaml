{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "hs-pylib-app.fullname" . }}
  labels:
    {{- include "hs-pylib-app.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "hs-pylib-app.selectorLabels" . | nindent 6 }}
{{- end }}
