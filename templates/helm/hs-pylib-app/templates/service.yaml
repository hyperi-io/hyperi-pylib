{{- if or .Values.service.http.enabled .Values.service.health.enabled .Values.service.metrics.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "hs-pylib-app.fullname" . }}
  labels:
    {{- include "hs-pylib-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "hs-pylib-app.selectorLabels" . | nindent 4 }}
  ports:
  {{- if and (eq .Values.app.type "api") .Values.service.http.enabled }}
  - name: http
    port: {{ .Values.service.http.port }}
    targetPort: http
    protocol: TCP
  {{- end }}
  {{- if .Values.service.health.enabled }}
  - name: health
    port: {{ .Values.service.health.port }}
    targetPort: health
    protocol: TCP
  {{- end }}
  {{- if .Values.service.metrics.enabled }}
  - name: metrics
    port: {{ .Values.service.metrics.port }}
    targetPort: metrics
    protocol: TCP
  {{- end }}
{{- end }}
