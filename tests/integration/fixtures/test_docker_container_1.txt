import sys
sys.path.insert(0, '/app/hs-lib/src')
from hs_lib.config import detect_environment, get_mount_config

env = detect_environment()
mounts = get_mount_config()

print(f"Environment: {env}")
print(f"Config dir: {mounts.config_dir}")
print(f"Data dir: {mounts.data_dir}")
print(f"Temp dir: {mounts.temp_dir}")
