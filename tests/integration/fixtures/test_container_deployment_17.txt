FROM python:3.11-slim

WORKDIR /app

# Install uv
RUN pip install --no-cache-dir uv

# Copy pyproject.toml and hyperi-pylib source
COPY pyproject.toml /app/
COPY src/hyperi-pylib /app/src/hyperi-pylib

# Install hyperi-pylib with api and metrics extras
RUN cd /app && uv pip install --system -e '.[api,metrics]'

# Copy application
COPY app_metrics.py /app/

# Expose both API and metrics ports
EXPOSE 8000 8080

ENV APP_NAME=metrics-test-api

CMD ["python", "app_metrics.py"]
