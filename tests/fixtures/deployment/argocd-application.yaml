# AUTOGENERATED -- do not edit by hand.
# Generated by hyperi_pylib.deployment.generate_argocd_application()
# Schema version: 2
# Source contract: dfe-api::deployment::contract()
# Regenerate with: `dfe-api generate-artefacts`
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dfe-api
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/hyperi-io/dfe-api
    targetRevision: main
    path: chart
    helm:
      releaseName: dfe-api

  destination:
    server: https://kubernetes.default.svc
    namespace: dfe

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
    - group: ""
      kind: Service
      jsonPointers:
        - /spec/clusterIP
        - /spec/clusterIPs
    - group: admissionregistration.k8s.io
      kind: ValidatingWebhookConfiguration
      jqPathExpressions:
        - .webhooks[].clientConfig.caBundle
